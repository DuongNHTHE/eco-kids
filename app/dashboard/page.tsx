'use client';

import { useEffect, useState } from 'react';
import Link from 'next/link';
import { useAPI } from '../../lib/hooks/useAPI';

export default function DashboardPage() {
  const { API } = useAPI();
  const [data, setData] = useState(null); const [topics, setTopics] = useState([]); const [done, setDone] = useState(false); const [menu, setMenu] = useState(false);
  useEffect(() => { const name = localStorage.getItem('eco-child') || 'Bé Mây'; Promise.all([API.get(`progress/${encodeURIComponent(name)}`, false, true, true), API.get('topics', false, true, true)]).then(([progress, nextTopics]) => { if (!progress.success && progress.message) return; setData({ ...progress, shortName: name.replace(/^Bé\s*/i, '') }); setTopics(nextTopics); }); }, [API]);
  if (!data) return <div className="grid min-h-screen place-items-center text-xl">Đang tải dashboard…</div>;
  const recent = data.records.slice(0, 3); const wordNames = { fox: ['🦊', 'Fox', 'Con cáo'], whale: ['🐋', 'Whale', 'Cá voi'], turtle: ['🐢', 'Turtle', 'Con rùa'], apple: ['🍎', 'Apple', 'Quả táo'], orange: ['🍊', 'Orange', 'Quả cam'], pear: ['🍐', 'Pear', 'Quả lê'], car: ['🚗', 'Car', 'Ô tô'], bus: ['🚌', 'Bus', 'Xe buýt'], rocket: ['🚀', 'Rocket', 'Tên lửa'] };
  return <div className="min-h-screen bg-[#f4faf4] text-[#203b35]"><aside className={`fixed inset-y-0 left-0 z-20 w-64 bg-[#203b35] p-6 text-white transition-transform lg:translate-x-0 ${menu ? 'translate-x-0' : '-translate-x-full'}`}><Link href="/" className="flex items-center gap-2 text-white"><span className="text-4xl font-extrabold text-[#f47d52]">eco</span><span className="border-l border-white/30 pl-2 text-xs font-black tracking-widest">KIDS<br /><small className="tracking-normal">Parent</small></span></Link><nav className="mt-12 space-y-2">{[['⌂', 'Tổng quan', '#overview'], ['▥', 'Tiến độ', '#progress'], ['☆', 'Hoạt động', '#activity']].map(([icon, label, href], index) => <a key={label} href={href} className={`flex gap-3 rounded-xl px-4 py-3 font-bold ${index === 0 ? 'bg-white/15' : ''}`}><span>{icon}</span>{label}</a>)}<Link href="/learn" className="flex gap-3 rounded-xl px-4 py-3 font-bold"><span>▶</span>Phòng học</Link></nav><div className="absolute bottom-8 rounded-2xl bg-white/10 p-4 text-sm"><b>💡 Cần trợ giúp?</b><p className="mt-2 text-white/70">Nhắn ECO-KIDS bất cứ lúc nào.</p><button className="mt-2 font-bold text-[#b7e0bd]">Trò chuyện</button></div></aside><main className="lg:ml-64" id="overview"><header className="flex items-center justify-between px-5 py-6 lg:px-10"><button onClick={() => setMenu(!menu)} className="text-2xl lg:hidden">☰</button><div><span className="text-sm font-bold text-[#80938a]">Góc đồng hành của ba mẹ</span><h1 className="text-3xl font-extrabold sm:text-4xl">Chào buổi tối, mẹ của <b>{data.shortName}</b> 👋</h1></div><div className="hidden items-center gap-3 rounded-full bg-white p-2 pr-5 shadow-sm sm:flex"><span className="text-3xl">👩🏻</span><span><b>Nguyễn Thu Hà</b><small className="block">Phụ huynh</small></span></div></header><div className="mx-auto max-w-7xl px-5 pb-10 lg:px-10"><section className="flex items-center justify-between overflow-hidden rounded-3xl bg-[#2d6358] p-7 text-white shadow-soft"><div><span className="font-bold text-[#b7e0bd]">Bản tin tuần này</span><h2 className="mt-2 text-4xl font-extrabold">{data.shortName} đang tiến bộ thật tuyệt!</h2><p className="mt-3 max-w-xl text-[#d1e4d4]">Con đã duy trì thói quen học đều và phát âm tốt hơn tuần trước.</p><Link href="/learn" className="mt-5 inline-block rounded-full bg-white px-5 py-3 font-bold text-[#203b35]">Tiếp tục bài học →</Link></div><div className="hidden text-8xl sm:block">🌱<span className="text-6xl">🦊</span></div></section><section id="progress" className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[['Aa', 'Từ đã học', data.stats.wordsLearned, 'green'], ['🔥', 'Chuỗi ngày học', `${data.stats.streak} ngày`, 'orange'], ['🎙', 'Điểm phát âm', `${data.stats.averageScore}%`, 'blue'], ['◷', 'Tổng thời gian', `${data.stats.totalMinutes} phút`, 'yellow']].map(([icon, label, value, color]) => <article key={label} className="rounded-2xl bg-white p-5 shadow-sm"><span className={`inline-grid h-11 w-11 place-items-center rounded-xl bg-${color}-100 font-black`}>{icon}</span><small className="mt-4 block text-[#80938a]">{label}</small><strong className="mt-1 block text-3xl font-extrabold">{value}</strong><span className="text-sm text-[#6eaa83]">↑ đang tiến bộ</span></article>)}</section><section className="mt-6 grid gap-6 xl:grid-cols-2"><article className="rounded-2xl bg-white p-6 shadow-sm"><small className="font-bold text-[#80938a]">Hoạt động học tập</small><h3 className="text-2xl font-extrabold">7 ngày gần đây</h3><div className="mt-8 flex h-48 items-end justify-around gap-3">{[6, 9, 5, 12, 8, 14, 10].map((value, index) => <div key={index} className="flex h-full flex-1 flex-col items-center justify-end gap-2"><i className={`w-full rounded-t-xl ${index === 6 ? 'bg-[#f47d52]' : 'bg-[#b7dcb9]'}`} style={{ height: `${value / 18 * 100}%` }} /><small>{['T2', 'T3', 'T4', 'T5', 'T6', 'T7', 'CN'][index]}</small></div>)}</div></article><article className="rounded-2xl bg-white p-6 shadow-sm"><div className="flex justify-between"><div><small className="font-bold text-[#80938a]">Hành trình chủ đề</small><h3 className="text-2xl font-extrabold">Khám phá của con</h3></div><Link href="/learn" className="font-bold">Học tiếp →</Link></div><div className="mt-5 space-y-4">{topics.map(topic => { const learned = new Set(data.records.filter(record => record.topicId === topic.id).map(record => record.wordId)).size; const percent = Math.round(learned / topic.words.length * 100); return <div key={topic.id} className="flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-xl text-2xl" style={{ background: `${topic.color}1f` }}>{topic.icon}</span><div className="flex-1"><b>{topic.vietnamese}</b><small className="block text-[#80938a]">{learned}/{topic.words.length} từ đã khám phá</small><div className="mt-2 h-2 rounded-full bg-[#e1ece1]"><i className="block h-full rounded-full" style={{ width: `${percent}%`, background: topic.color }} /></div></div><b>{percent}%</b></div>; })}</div></article><article id="activity" className="rounded-2xl bg-white p-6 shadow-sm"><small className="font-bold text-[#80938a]">Những từ mới nhất</small><h3 className="text-2xl font-extrabold">Con vừa học</h3><div className="mt-5 space-y-3">{recent.length ? recent.map(record => { const item = wordNames[record.wordId] || ['✨', record.wordId, 'Từ mới']; return <div key={record._id || record.wordId} className="flex items-center gap-3 rounded-xl bg-[#f4faf4] p-3"><span className="text-2xl">{item[0]}</span><span className="flex-1"><b>{item[1]}</b><small className="block text-[#80938a]">{item[2]}</small></span><b className="text-[#6eaa83]">{record.score}%</b></div>; }) : <p className="text-[#80938a]">Con chưa học từ nào. Hãy bắt đầu bài đầu tiên nhé!</p>}</div></article><article className="rounded-2xl bg-[#fff2d5] p-6"><span className="text-4xl">💡</span><small className="mt-3 block font-bold text-[#9a7d3a]">Gợi ý cho tối nay</small><h3 className="text-2xl font-extrabold">Săn tìm đồ vật trong nhà</h3><p className="mt-2 text-[#74643f]">Ba mẹ gọi tên một màu bằng tiếng Anh, bé có 30 giây để tìm một đồ vật đúng màu. Không màn hình, vẫn ôn bài thật vui!</p><button onClick={() => setDone(true)} className="mt-4 rounded-full bg-[#203b35] px-5 py-3 font-bold text-white">{done ? 'Đã hoàn thành hôm nay 🌟' : 'Đánh dấu đã chơi ✓'}</button></article></section></div></main></div>;
}